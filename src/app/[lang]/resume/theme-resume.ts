'use client';

import { createTheme } from '@mui/material';

export const themeResume = createTheme({
  typography: {
    h1: {
      fontSize: '1rem',
      color: 'blue',
    },
    h2: {
      color: 'green',
    },
  },
});

export default themeResume;
